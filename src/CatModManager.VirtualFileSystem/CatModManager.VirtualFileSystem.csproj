<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
      <_Parameter1>CatModManager.Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>

  <!--
    No VFS packages here on purpose. Mono.Fuse.NETStandard went with the retired FUSE driver, and
    winfsp.net was a leftover from the abandoned Windows VFS experiment — still shipped in the
    binary even though no code referenced it. Deployment is hard links on every platform now.
  -->

</Project>
